---
name: demo-skill
description: Normalize newline-delimited JSON files into deterministic JSON arrays. Use when a user requests NDJSON normalization or canonical JSON conversion.
---

# Demo Skill

1. Inspect the input schema.
2. Preserve values while sorting object keys.
3. Validate the normalized JSON before delivery.
