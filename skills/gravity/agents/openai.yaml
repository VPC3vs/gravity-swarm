interface:
  display_name: "Gravity"
  short_description: "Lean GPT-routed orchestration for coding"
  default_prompt: "Use $gravity to choose /orbit, /lens, /accrete, or /bigbang and delegate only when independent work justifies it."

policy:
  allow_implicit_invocation: true
