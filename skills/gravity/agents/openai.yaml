interface:
  display_name: "Gravity · Astra"
  short_description: "Astra-only orchestration with low, medium, and ultra"
  default_prompt: "Use $gravity with GPT-6 Astra only: ultra for the coordinator, medium for substantial workers, and low for routine workers."

policy:
  allow_implicit_invocation: true
